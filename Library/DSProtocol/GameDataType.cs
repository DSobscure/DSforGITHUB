namespace DSProtocol
{
    public enum MoveType
    {
        Move,
        Rotate
    }
    public enum MoveDirection
    {
        MoveFoward,
        MoveBackWard,
        RotateLeft,
        RotateRight
    }
}
